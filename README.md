# Copilot Agent Demo

A small, dependency-free Python command-line calculator demonstrating addition, subtraction, multiplication, and division with a zero-division check.

## Run

```bash
python main.py
```

Follow the prompts to enter two numbers and an operation (`+`, `-`, `*`, or `/`).